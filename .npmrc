; @typescript-eslint/typescript-estree (an optional dep) peer-requires an older
; typescript than the one this project builds with. yarn 1.x never enforced peer
; deps, so this was always tolerated; keep npm lenient to match that behavior.
legacy-peer-deps=true
